

```c
#include <iostream>
#include <cstring>       
#include <string>

using namespace std;

int main() {
  char c[101] = {0, }; char r[101] = {0, };
  int n; int check = 0;
  
  scanf("%s",c);
  scanf("%d", &n);
  
  for(int i=0;i < n; i++)
    {
      scanf("%s",r);
      if(strcmp(c,r) == 0)
      {
        printf("Yes");
        return 0;
      }
    }
  
  printf("No");
  return 0;
}

```
